# Frozen, fully-pinned snapshot of the venv (Part 10) -- direct AND transitive
# dependencies, generated via `uv pip freeze --python ./.venv/Scripts/python.exe`.
#
# This is what the Dockerfile installs from, not requirements.txt -- the image
# should be bit-for-bit reproducible across builds, which floor versions (>=)
# in requirements.txt don't guarantee (a rebuild months later could silently
# resolve different transitive versions of things we never listed directly,
# e.g. starlette, anyio, pydantic-core). requirements.txt stays the
# human-edited manifest (add/remove a dependency there); regenerate this file
# with the same freeze command whenever requirements.txt changes.

annotated-doc==0.0.4
annotated-types==0.7.0
anthropic==0.117.0
anyio==4.14.2
certifi==2026.6.17
click==8.4.2
colorama==0.4.6
distro==1.9.0
docstring-parser==0.18.0
fastapi==0.139.2
h11==0.16.0
httpcore==1.0.9
httpcore2==2.7.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.7.0
idna==3.18
iniconfig==2.3.0
jiter==0.16.0
packaging==26.2
pluggy==1.6.0
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.2
pygments==2.20.0
pytest==9.1.1
python-dotenv==1.2.2
pyyaml==6.0.3
sniffio==1.3.1
starlette==1.3.1
tenacity==9.1.4
truststore==0.10.4
typing-extensions==4.16.0
typing-inspection==0.4.2
uvicorn==0.51.0
watchfiles==1.2.0
websockets==16.1.1
